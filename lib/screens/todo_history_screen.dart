import 'package:flutter/material.dart';

class TodoHistoryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Todo History')),
      body: Center(child: Text('Todo History Screen')),
    );
  }
}
